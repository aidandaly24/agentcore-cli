[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{ PackageName }}"
version = "0.1.0"
description = "AgentCore Lambda Interceptor — pass-through"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["."]
